<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yqcui.yqweb.mapper.UserMapper">
    <!-- define the result map for the User class -->
    <resultMap type="User" id="userMap">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="password" column="password"/>
        <result property="dob" column="dob"/>
    </resultMap>

    <resultMap type="Order" id="orderMap">
        <id property="id" column="id"/>
        <result property="orderTime" column="orderTime"/>
        <result property="uid" column="userId"/>
    </resultMap>

    <!-- define the SQL queries for the UserMapper -->
    <select id="selectById" resultMap="userMap">
       select * from User where id = #{id}
    </select>

    <select id="selectAllUserAndOrders" resultMap="orderMap">
        SELECT o.id, o.orderTime, u.id as userId
        FROM `Order` o JOIN User u ON o.uid = u.id
    </select>

</mapper>
